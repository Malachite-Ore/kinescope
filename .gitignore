# Virtual environment
.venv/
venv/
ENV/

# Python caching and compiled files
__pycache__/
*.pyc
*.pyo
*.pyd

# PyInstaller build directories and specs
build/
dist/
*.spec
icon.ico
icon.icns

# Node.js dependencies and builds
node_modules/
frontend/node_modules/
frontend/dist/

# Log files and local binaries downloaded at runtime
*.log
bin/

# Local user data written at runtime — never publish these.
# settings.json holds the download directory and cookie file path;
# history.json holds the titles and URLs of everything downloaded.
/settings.json
/history.json

# Local Claude Code guidance — keep out of the public repo
CLAUDE.md
